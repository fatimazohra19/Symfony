{% extends 'base.html.twig' %}
{% block title "Toutes les recettes" %} 
{% block body %}
<p>je teste body </p>
    <p>
    <a class="btn btn-primary btn-sm" href="{{ path('recipe.create') }}">Créer une recette</a>

    </p>
    <table class="table">
   <thead>
    <tr>
    <th>Titre</th>
    <th>Actions</th>
    </tr>
   </thead>
<tbody>
    {% for recipe in recipes %}
    <tr>
        <td>
      
    <a href="{{ path('recipe.show', {id: recipe.id, slug: recipe.slug|replace({' ': '-'}) }) }}">{{ recipe.getTitle() }}</a>

    </td>
        <td>
        <a class="btn btn-primary" href="{{ path('recipe.edit',{id: recipe.id}) }}">Editer</a>
        </td>
    </tr> 
    {% endfor %}
</ul>
</tbody>
<ul>

</table>
{% endblock %}